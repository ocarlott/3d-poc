import * as THREE from 'three';
export declare class DecalGeometry extends THREE.BufferGeometry {
    constructor(camera: THREE.Camera, mesh: THREE.Mesh, position: THREE.Vector3, orientation: THREE.Euler, size: THREE.Vector3);
}
